import styled from "styled-components";

const Index = ({ next }: { next: () => Promise<boolean> }) => {
  return <PaymentBlock>결제화면</PaymentBlock>;
};

const PaymentBlock = styled.article``;

export default Index;
